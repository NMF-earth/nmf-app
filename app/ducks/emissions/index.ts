import actions from "./emissions.slice";
import namespace from "./emissions.slice";
import reducer from "./emissions.slice";
import selectors from "./emissions.selectors";

export default { actions, namespace, reducer, selectors };
