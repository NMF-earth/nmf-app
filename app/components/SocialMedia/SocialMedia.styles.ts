import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
  box: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    maxWidth: 200,
    gap: 40,
    paddingBottom: 50,
  },
});
