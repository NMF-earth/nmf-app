import en from "./en";
import fr from "./fr";
import de from "./de";
import sv from "./sv";

interface TranslationKeys {
  TRAIN: string;
  CAR: string;
  PLANE: string;
  BOAT: string;
  BUS: string;
  RED_MEAT: string;
  WHITE_MEAT: string;
  CUSTOM: string;
}

export { en, fr, de, sv, TranslationKeys };
