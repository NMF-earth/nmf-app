import { StyleSheet } from "react-native";

export default StyleSheet.create({
  view: {
    flex: 1,
    margin: 40,
    alignItems: "center",
    justifyContent: "center",
  },
  animationImage: {
    height: 200,
    width: 200,
  },
});
