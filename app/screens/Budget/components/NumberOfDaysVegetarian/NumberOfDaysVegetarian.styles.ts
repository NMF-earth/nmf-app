import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 10,
    marginBottom: 26,
    flex: 1,
    alignItems: "center"
  },
  daysContainer: {
    flex: 1,
    flexDirection: "row"
  }
});

export default styles;
