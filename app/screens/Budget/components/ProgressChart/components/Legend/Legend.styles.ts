import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    justifyContent: "space-between",
    flexWrap: "wrap",
    flexDirection: "row",
    paddingVertical: 20,
  },
});
